import React from 'react'

function Header () {
  return (
    <header style={headerStyle}>
      <h1>Cancel an Application</h1>
    </header>
  )
}

const headerStyle = {
  background: '#fff',
  color: '#000',
  textAlign: 'left'
}

export default Header
